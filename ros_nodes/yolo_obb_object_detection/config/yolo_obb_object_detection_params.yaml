/**:
  ros__parameters:
    yolo_model: 'best.pt'
    model_conf: 0.1
    color_image_sub_topic: '/fls_image/display_mono'
    yolo_detections_pub_topic: '/yolo_obb_object_detection/detections'
    yolo_annotated_pub_topic: '/yolo_obb_object_detection/annotated'
    device: 'cpu' # Inference device: 'cpu', 0, 1, 'cuda', 'cuda:0', or 'mps' (Mac GPU)
    imgsz: [1984, 1088] # Inference image size [width, height]
