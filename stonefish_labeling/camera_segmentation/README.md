## Camera Segmentation
Node that subscribes to color image and segmentation camera from the stonefish simulator and saved the corresponding image, mask pair to a file to generate dataset.

Note: topics from the simulator are not exactly synchronized. Set the max allowed time offset in ms in the config.
